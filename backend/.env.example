NODE_ENV=development
PORT=5000
DISABLE_IAP_VALIDATION=true

# Use SQLite for local development (no database setup needed)
USE_MYSQL=false

# Google Cloud Configuration (for development, these are optional)
GCP_PROJECT_ID=komo-infra-479911
GCP_KEY_FILE=./service-account-key.json
GCP_REGION=us-central1

# Cloud SQL Configuration (Local MySQL) - Only used if USE_MYSQL=true
CLOUD_SQL_HOST=34.136.51.9
CLOUD_SQL_USER=postgres
CLOUD_SQL_PASSWORD=c*KiH8#71Ol>SrL.
CLOUD_SQL_DATABASE=video_metadata
CLOUD_SQL_PORT=5432

# Cloud Storage Configuration
GCS_BUCKET_NAME=play-video-upload-01

# Pub/Sub Configuration
PUBSUB_TOPIC=eventarc-us-central1-my-trigger-538
    PUBSUB_SUBSCRIPTION=eventarc-us-central1-my-trigger-sub-807

# Vertex AI Configuration
VERTEX_AI_MODEL_ID=powerplaystream_1764668948865

# JWT Configuration
JWT_SECRET=dev-secret-key-for-development-only-change-in-production
JWT_EXPIRY=7d

# API Configuration
    REACT_APP_API_URL=https://looply.co.in/api
